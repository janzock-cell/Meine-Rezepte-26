{
  "name": "azubi-rezepte-sammlung",
  "version": "1.1.0",
  "private": true,
  "description": "A hybrid local/cloud recipe generator for apprentices.",
  "scripts": {
    "dev": "esbuild index.tsx --bundle --outfile=index.js --format=esm --watch",
    "build": "esbuild index.tsx --bundle --outfile=index.js --format=esm --minify"
  },
  "dependencies": {
    "@google/genai": "^0.14.0",
    "firebase": "^10.8.0",
    "cropperjs": "^1.6.1",
    "canvas-confetti": "^1.9.2"
  },
  "devDependencies": {
    "@netlify/functions": "^2.7.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "netlify-cli": "^17.23.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "@types/jest": "^29.5.12"
  }
}